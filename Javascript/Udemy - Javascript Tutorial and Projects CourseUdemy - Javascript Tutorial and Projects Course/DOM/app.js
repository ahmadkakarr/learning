// Select the element or group of elements the we want
// Decide the effect we want to apply to the selection

/*
document.body.style.color = 'green';

document.querySelector('h2').style.color = 'red';



//window object: use chrome browser
console.log(window)
//to view the document
console.log(document)
// to find the object properties and methods
console.dir(document)

const btns = document.getElementById('btn')
btns.style.color = 'red';
btns.style.border = 'solid';

const name = btns.nodeName;
const css  = btns.style;

console.log(btns);
console.log(name);
console.log(css);

*/

/*

// getElementsByTagName('tagname');
// node-list =  array-like object
// index, length property but not array methods

const headings = document.getElementsByTagName('h2');
headings[0].style.color = 'red';
// console.log(headings.length);

const items = document.getElementsByTagName('li');

//for each dose not work on node-list
// items.forEach(function(item) {
//   console.log(item);
// });
items[2].style.color = 'orange';

// converting note-list into an array
const betterItems = [...items];

// =============================================
// for selecting each item in array with forEach
// =============================================
betterItems.forEach(function(item) {
  // console.log(item);
});
console.log(items);
console.log(betterItems);

const headings2 = document.getElementsByTagName('h2');
console.log(headings2);
console.log(headings2.length);

headings2[1].style.color = 'blue';
headings2[2].style.color = 'green';

*/






